import "./globals.css";

export const metadata = {
  title: "Discord Integration Transcript",
  description: "Discord Integration Transcript",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-br">
      <body>{children}</body>
    </html>
  );
}
